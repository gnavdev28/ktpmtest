Phát biểu nào sau đây là đúng về các phần tử bên trong một phân hệ (subsystem) và phạm vi truy cập công khai (public visibility)?
* Chỉ tập hợp con các phần tử định nghĩa giao diện (API) của phân hệ mới nên để công khai (public).
Chỉ lớp đại diện (proxy class) của phân hệ mới nên để công khai (public).
Không phần tử nào bên trong phân hệ được để công khai (public).
Chỉ các phần tử liên kết đến lớp bên ngoài mới nên để công khai (public).

Hai loại phụ thuộc (dependency) nào có thể được sử dụng từ một phân hệ (subsystem)? (Chọn hai.)
* Phụ thuộc <<uses>> đến một giao diện (interface) của phân hệ khác.
* Phụ thuộc <<import>> đến một gói (package) chứa các lớp cần sử dụng.
Quan hệ <<manifest>> đến một nút (node) trong sơ đồ triển khai.
Quan hệ <<realize>> đến một hoặc nhiều sự hợp tác (collaboration occurrences).

Nhiệm vụ nào sau đây được thực hiện trong bước tinh chỉnh cách chạy tính năng (use-case realization refinement)?
Xác định các lớp tham gia thiết kế.
* Phân bổ nhiệm vụ và trách nhiệm cho từng lớp cụ thể.
Mô hình hóa các tin nhắn truyền giữa các lớp.
Mô hình hóa mối quan hệ liên kết giữa các lớp.

Phát biểu nào sau đây là đúng về phân hệ thiết kế (design subsystems)?
Chúng đóng gói một phần nhỏ hành vi của hệ thống.
* Chúng đại diện cho một chức năng hoạt động độc lập và giao tiếp qua các giao diện rõ ràng.
Chúng mô hình hóa một phương án cài đặt phần mềm duy nhất.
Chúng chỉ được phép chứa các lớp thiết kế thông thường.

Cho cấu hình sau: Gói A chứa lớp aClass ở tầng giao diện (presentation layer). Gói B chứa lớp bClass và giao diện bInterface ở tầng nghiệp vụ (business layer). Gói C chứa lớp cClass ở tầng dữ liệu (data layer). Thiết kế nào sau đây là tồi?
aClass gọi trực tiếp một phương thức trong bClass.
* aClass chứa một thuộc tính có kiểu là cClass.
aClass hiện thực hóa (realizes) bInterface.
bClass hiện thực hóa (realizes) bInterface.

Tài liệu quy trình nào mô tả các cơ chế thiết kế (design mechanisms), cách ánh xạ và chi tiết sử dụng chúng?
* Tài liệu Kiến trúc Phần mềm (Software Architecture Document)
Hướng dẫn Thiết kế (Design Guidelines)
Tài liệu Tầm nhìn (Vision Document)
Kế hoạch Phát triển Phần mềm (Software Development Plan)

Trong sơ đồ máy trạng thái, hành vi của một trạng thái có thể được kích hoạt vào những thời điểm nào? (Chọn ba.)
Trước khi đối tượng đạt đến trạng thái đó.
* Khi vừa bước vào trạng thái (entry).
* Khi vừa thoát khỏi trạng thái (exit).
* Khi đang nằm ở trong trạng thái đó (do).

Cổng (gate) là gì?
* Một tham số đại diện cho tin nhắn đi qua ranh giới của một sơ đồ tương tác hoặc một phân đoạn tương tác.
Một giao thức bảo mật dùng để truy cập vào bên trong phân hệ.
Một điểm quyết định trong máy trạng thái có nhiều hơn hai lựa chọn rẽ nhánh.
Một tập hợp các tiêu chí kiểm tra bắt buộc mà phân hệ thiết kế phải đạt được trước khi viết code.

Khi chuyển đổi từ phân tích sang thiết kế, một lớp phân tích (analysis class) đơn giản sẽ được chuyển thành một:
Lớp hoạt động (active class)
Giao diện (interface)
* Lớp thiết kế (design class)
Phân hệ thiết kế (subsystem)

Trong hoạt động nào của quy trình thiết kế thì cơ chế phân phối (distribution mechanism) được xác định?
Xác định các phần tử thiết kế (Identify Design Elements)
Xác định các cơ chế thiết kế (Identify Design Mechanisms)
Thiết kế lớp chi tiết (Class Design)
* Phân tích kiến trúc hệ thống (Architectural Analysis)

[image: SAD/images/page_2_img_1.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu E đại diện cho cái gì?
Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
* Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)

Hoạt động Xác định các phần tử thiết kế (Identify Design Elements) thuộc bước công việc nào?
Định nghĩa một Khung Kiến trúc Ứng viên
Thiết kế các thành phần hệ thống
Thực hiện phân tích kiến trúc hệ thống
* Tinh chỉnh kiến trúc hệ thống (Refine the Architecture)

[image: SAD/images/page_3_img_1.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu H đại diện cho cái gì?
Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
* Điều kiện bảo vệ (guard condition)

Mối quan hệ giữa thao tác (operation) và phương thức (method) là gì?
Hai thuật ngữ này có nghĩa hoàn toàn giống nhau.
Một thao tác mô tả cách phương thức được lập trình chi tiết như thế nào.
* Một phương thức chứa đoạn code chi tiết để thực hiện cho một thao tác đã khai báo.
Giữa chúng không có mối quan hệ nào.

Tại sao bạn nên dùng giao diện phân hệ (subsystem interface) thay vì chính phân hệ đó trên các sơ đồ tuần tự?
Để vẽ phân hệ dễ dàng hơn trong bước Thiết kế Phân hệ.
* Để giúp sơ đồ thiết kế dễ sửa đổi hơn khi thay đổi code bên trong phân hệ.
Để giảm bớt việc sửa đổi sơ đồ tuần tự khi các hàm của lớp thay đổi.
Để giảm số lượng lớp cần lập trình để chạy phân hệ.

Sản phẩm đầu vào nào là cần thiết cho hoạt động Xác định các Phần tử Thiết kế (Identify Design Elements)?
Mô hình triển khai phần cứng (Deployment Model)
Mô hình code thực tế (Implementation Model)
Kiến trúc tham chiếu có sẵn (Reference Architecture)
* Tài liệu kiến trúc phần mềm (Software Architecture Document)

Một lưu ý quan trọng khi phân chia các phần mềm chạy trên các máy tính (nodes) khác nhau là gì?
* Giảm thiểu tối đa lượng dữ liệu truyền qua mạng để tránh nghẽn mạng.
Giảm thiểu điện năng tiêu thụ của các máy tính.
Phải tận dụng hết tất cả các máy tính đang có trong phòng.
Khoảng cách vật lý (mét) giữa các máy tính.

Đường kết nối (connector) trên sơ đồ triển khai phần cứng đại diện cho loại cơ chế nào?
Cơ chế sao lưu dữ liệu (backup)
* Cơ chế truyền thông tin qua mạng (communication)
Cơ chế quản lý giao dịch (transaction)
Cơ chế tính toán dữ liệu (computation)

Một cơ chế thiết kế (design mechanism) có đặc điểm nào?
Chỉ mô tả ý tưởng chung của giải pháp mà không quan tâm đến công nghệ cài đặt.
Quy định rõ ràng loại công nghệ, ngôn ngữ lập trình hoặc hãng phần mềm cụ thể sẽ dùng.
Nó hoàn toàn giống với một mẫu thiết kế (design pattern).
* Đã giả định trước một số đặc điểm của môi trường chạy, nhưng chưa chốt cứng công nghệ cụ thể của hãng nào.

Khi xác định các giao diện (interfaces) trong thiết kế, phát biểu nào sau đây là đúng?
Các lớp thiết kế bình thường không nên thực hiện giao diện nào.
Mỗi phân hệ thiết kế chỉ được phép thực hiện đúng một giao diện.
Phải tạo các giao diện trước khi tạo ra các phân hệ.
* Các giao diện nên được đóng gói riêng biệt, tách rời khỏi các lớp thực hiện chúng.

Các phân hệ (subsystems) mới có thể được phát hiện trong thiết kế ca sử dụng bằng cách chú ý:
* Các đoạn giao tiếp chung giữa các đối tượng xuất hiện ở nhiều sơ đồ tuần tự khác nhau.
Các đối tượng giống nhau xuất hiện trên nhiều sơ đồ tuần tự.
Chuỗi chuyển đổi trạng thái giống nhau của nhiều lớp khi chạy tính năng.
Các lớp thiết kế giống nhau cùng tham gia vào nhiều tính năng khác nhau.

Những công việc nào được thực hiện trong bước Thiết kế Ca sử dụng (Use Case Design)? (Chọn hai.)
Chuyển đổi lớp phân tích thành lớp thiết kế và phân hệ thiết kế.
* Thiết kế cách dữ liệu được lưu trữ lâu dài vào cơ sở dữ liệu (persistence).
* Thiết kế chi tiết các tin nhắn truyền giữa các đối tượng để chạy các hàm giao diện.
Đơn giản hóa sơ đồ tuần tự bằng cách dùng các lớp thiết kế.

Cái gì được dùng để đại diện cho một phân hệ (subsystem) trên sơ đồ tuần tự?
Một giao diện (interface) mà phân hệ đó thực hiện.
* Một đối tượng đại diện của phân hệ (subsystem proxy).
Một thành phần (component) của phân hệ.
Một lớp (class) thông thường nằm trong phân hệ.

Thành phần UML nào dùng để vẽ kiến trúc vật lý và mạng của hệ thống?
Các lớp thiết kế và các mối quan hệ liên kết.
Các đối tượng và các tin nhắn truyền qua lại.
Các phân hệ và các quan hệ phụ thuộc.
* Các nút thiết bị (nodes) và đường kết nối mạng (connectors).

Sơ đồ nào sau đây được dùng để vẽ chi tiết cách các đối tượng tương tác chạy tính năng (use-case realizations)?
Tài liệu mô tả tính năng bằng chữ viết.
* Sơ đồ truyền thông (communication diagrams).
Biểu đồ máy trạng thái (state charts).
Sơ đồ hoạt động (activity diagrams).

Cái gì định nghĩa các nhiệm vụ mà một phân hệ (subsystem) bắt buộc phải thực hiện?
Cách viết code hoạt động của các lớp bên trong nó.
* Các hàm đã khai báo trong các giao diện (interfaces) mà nó cam kết thực hiện.
Các tính năng hệ thống có sự tham gia của phân hệ đó.
Các hàm khai báo trong một lớp chứa trong phân hệ đó.

Cơ chế nào sau đây là một cơ chế thiết kế (design mechanism)?
* Lưu trữ dữ liệu lâu dài (Persistency)
Cơ sở dữ liệu hướng đối tượng của hãng ObjectStore
Cơ chế phân phối qua mạng (Distribution)
Triệu gọi hàm từ xa (Remote Method Invocation)

Ba bước trong quá trình Phân loại các Cơ chế Phân tích thành cơ chế thiết kế là gì? (Chọn ba.)
* Xác định yêu cầu đặc trưng cho từng cơ chế.
* Xác định các lớp đối tượng cần sử dụng dịch vụ đó.
Chỉ định phần mềm cụ thể của hãng thứ ba cho mỗi cơ chế.
* Gom nhóm các lớp đối tượng có chung yêu cầu đặc trưng giống nhau.

Trong Thiết kế Phân hệ, điều gì xảy ra ở bước Phân phối trách nhiệm của phân hệ (Distribute Subsystem Responsibilities)?
* Chia nhỏ và giao các nhiệm vụ của phân hệ cho các lớp con bên trong nó thực hiện.
Kiểm tra xem nhiệm vụ của phân hệ có đồng bộ không, chuyển nhiệm vụ không liên quan sang phân hệ khác.
Xác định các thư viện ngoài để chạy phân hệ.
Chi tiết hóa cơ chế mạng để hiển thị giao diện phân hệ.

Thực thể nào có ranh giới rõ ràng, có định danh duy nhất, tự quản lý dữ liệu (state) và code (behavior)?
Lớp (class)
* Đối tượng (object)
Thành phần (component)
Gói (package)

Mục đích của hoạt động Xác định các Cơ chế Thiết kế (Identify Design Mechanisms) là gì?
Tinh chỉnh cơ chế và chọn chính xác công nghệ cụ thể của hãng thứ ba để cài đặt.
Cung cấp các dịch vụ mức khái niệm cho các đối tượng phân tích sử dụng.
* Tinh chỉnh cơ chế khái niệm thành cơ chế thiết kế dựa trên các ràng buộc thực tế của môi trường chạy.
Tạo ra các thành phần tạm thời để kiến trúc sư không bị mất tập trung khi thiết kế.

Trong quan hệ phụ thuộc (dependency) tạm thời, lớp gọi (client) nhận biết được lớp bị gọi (supplier) qua những cách nào? (Chọn ba.)
* Được truyền vào làm tham số của hàm (parameter reference).
* Khai báo làm biến cục bộ trong hàm (local reference).
* Là biến toàn cục của hệ thống (global reference).
Khai báo làm thuộc tính của lớp (field reference).

Trong bước thiết kế nào, các phân hệ được tạo ra từ việc gom nhóm các lớp phân tích?
Phân tích kiến trúc hệ thống (Architectural Analysis)
* Xác định các phần tử thiết kế (Identify Design Elements)
Xác định các phân hệ thiết kế
Tận dụng các thành phần thiết kế sẵn có

Phần tử thiết kế nào dùng để biểu diễn một đối tượng chạy độc lập, song song (concurrent object)?
* Lớp hoạt động (active class)
Kén bảo vệ (capsule)
Lớp thiết kế thông thường (design class)
Sự kiện kích hoạt (event)

Hoạt động Mô tả Phân phối (Describe Distribution) sẽ phân chia các tiến trình phần mềm chạy trên:
* Các máy tính/thiết bị vật lý (physical nodes).
Các thành phần phần mềm (components).
Các lớp thiết kế (classes).
Các hoạt động của hệ thống (activities).

Trong Thiết kế Phân hệ, nên vẽ bao nhiêu sơ đồ tương tác (sơ đồ tuần tự hoặc sơ đồ truyền thông)?
* Vẽ ít nhất một sơ đồ tương tác cho mỗi hàm khai báo ở giao diện.
Vẽ một sơ đồ tương tác cho mỗi hiện thực hóa giao diện.
Vẽ ít nhất một sơ đồ tương tác cho mỗi lần gọi giao diện bên ngoài.
Vẽ một sơ đồ tương tác cho mỗi lớp thiết kế bên trong.

Sơ đồ biểu diễn các trạng thái và các mũi tên chuyển trạng thái của đối tượng là sơ đồ:
Sơ đồ truyền thông (communication diagram)
Sơ đồ tuần tự (sequence diagram)
Sơ đồ thành phần (component diagram)
* Sơ đồ máy trạng thái (state machine diagram)

[image: SAD/images/page_7_img_1.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu F đại diện cho cái gì?
Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
* Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)

Thiết kế Ca sử dụng (Use Case Design) thuộc bước công việc nào sau đây?
Thiết kế ca sử dụng (Design Use Cases)
Phân tích hành vi hệ thống (Analyze Behavior)
* Thiết kế các thành phần hệ thống (Design Components)
Thiết kế các lớp và phân hệ

Khi nào một lớp phân tích (analysis class) được chuyển đổi trực tiếp thành một lớp thiết kế tỷ lệ 1:1?
Khi lớp phân tích được gắn nhãn <<entity>>.
* Khi lớp phân tích chỉ đại diện cho một khái niệm logic đơn giản, duy nhất.
Khi công cụ vẽ UML tự động hỗ trợ chuyển đổi từ Phân tích sang Thiết kế.
Khi nhà phân tích có kỹ năng viết code tốt.

Mục đích chính của một phân hệ thiết kế (design subsystem) là gì?
Quản lý cấu hình phần mềm và sắp xếp các tệp thiết kế.
* Che giấu các xử lý phức tạp bên trong và chỉ để lộ giao diện ra ngoài.
Gom nhóm các lớp thiết kế giống nhau lại một chỗ.
Đại diện cho các phần mềm bên ngoài hệ thống.

Mục đích của việc thiết kế phân hệ (subsystem design) là gì?
Hoàn thiện chi tiết của từng giao diện trong ứng dụng.
Chia nhỏ hệ thống thành nhiều phần để giao việc cho các nhóm lập trình khác nhau.
* Thiết kế chi tiết cách các lớp con bên trong phân hệ hợp tác chạy các hàm của giao diện phân hệ đó.
Quy định phân hệ nào chạy trên máy chủ nào và giao thức kết nối giữa chúng.

[image: SAD/images/page_10_img_1.jpeg]
Trong sơ đồ, C1 và C2 đại diện cho cái gì?
Nhánh rẽ song song (forks)
Trạng thái bắt đầu (initial states)
* Điểm quyết định rẽ nhánh (decisions)
Mũi tên chuyển trạng thái (transitions)
Trạng thái kết thúc (final states)
Sự kiện kích hoạt (events)
Trạng thái thông thường (states)
Điều kiện bảo vệ (guard conditions)

Định nghĩa cấu hình kết nối mạng là bước thứ mấy của hoạt động Mô tả Phân phối (Describe Distribution)?
Bước cuối cùng (final)
* Bước đầu tiên (first)
Bước thứ hai (second)
Bước thứ năm (fifth)

[image: SAD/images/page_9_img_1.jpeg]
Trong sơ đồ hoạt động, ký hiệu B đại diện cho cái gì?
* Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)

Phát biểu nào sau đây là đúng về các gói (packages) và phân hệ (subsystems)?
Một gói không được chứa phân hệ.
Một gói tự cung cấp hành vi chạy phần mềm.
* Một phân hệ tự cung cấp hành vi chạy phần mềm.
Bạn sử dụng một gói khi bạn muốn che giấu và đóng gói hành vi phần mềm.

Có bao nhiêu nút vật lý (physical nodes) cần xác định để thực hiện hoạt động Mô tả Phân phối (Describe the Distribution)?
Bắt buộc không có nút nào.
Bắt buộc phải có đúng một nút.
* Không cần nút nào hoặc có một nút.
Bắt buộc phải có nhiều hơn một nút.

Các ghi chú (notes) và kịch bản (scripts) bổ sung trên sơ đồ tuần tự thường dùng để làm gì? (Chọn ba.)
* Quy định thời gian bắt buộc giữa các tin nhắn truyền đi.
* Vẽ chi tiết các luồng xử lý có điều kiện rẽ nhánh.
Khai báo thuộc tính cho các đối tượng vẽ trên sơ đồ.
* Khớp các điểm mở rộng ca sử dụng với vị trí tương ứng trên sơ đồ tuần tự.

Cái gì được dùng để hướng dẫn lập trình viên áp dụng cơ chế phân phối khi viết code?
Sơ đồ hoạt động.
Sơ đồ luồng dữ liệu (flowchart).
* Mẫu UML thiết kế sẵn kèm tài liệu hướng dẫn bằng chữ viết.
Sơ đồ ca sử dụng (use-case diagram).

[image: SAD/images/page_11_img_1.png]
Dựa vào sơ đồ lớp của lớp Động vật (Animal), Sư tử (Lion) và Hổ (Tiger), phát biểu nào đúng?
Sư tử và Hổ gọi trực tiếp phương thức của nhau.
Sư tử và Hổ gửi tin nhắn trực tiếp cho Động vật.
* Lớp Động vật (Animal) là lớp trừu tượng nên có thể không có đối tượng cụ thể nào của nó chạy trong bộ nhớ.
Đối tượng Động vật giao tiếp khác đối tượng Sư tử hoặc Hổ.

Một thuộc tính được gạch chân dưới tên trong sơ đồ lớp biểu thị điều gì?
Thuộc tính đó chỉ được đọc, không được sửa.
Giá trị thuộc tính được tính toán từ thuộc tính khác.
Thuộc tính dùng để định danh duy nhất cho đối tượng.
* Thuộc tính tĩnh (static) thuộc về cả lớp chứ không riêng biệt cho từng đối tượng.

[image: SAD/images/page_12_img_1.jpeg]
Trong sơ đồ lớp, thuộc tính CourseOffering.numberOfStudents được gạch chân là ví dụ về:
Một thiết kế tồi.
* Thuộc tính tĩnh (static attribute) của lớp.
Thuộc tính được tính toán từ giá trị khác (derived attribute).
Cách đặt tên thuộc tính không chuẩn.

[image: SAD/images/page_12_img_2.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu A đại diện cho cái gì?
Nhánh rẽ song song (fork)
* Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)

Trong lưu trữ cơ sở dữ liệu (persistence), hai chức năng chính của giao dịch (transactions) là gì? (Chọn hai.)
* Đảm bảo một nhóm các câu lệnh SQL chạy hết toàn bộ hoặc hủy bỏ toàn bộ (không chạy gì cả).
Giúp lập trình viên ưu tiên chạy các hàm quan trọng khi máy chủ hết tài nguyên.
* Đảm bảo dữ liệu chuyển từ trạng thái đồng bộ này sang trạng thái đồng bộ khác không bị lỗi giữa chừng.
Giúp lập trình viên không cần suy nghĩ về lưu trữ dữ liệu khi thiết kế ca sử dụng.
Cung cấp các cách vẽ chuẩn hóa đối tượng lưu trữ trong UML.

Ba mục đích chính của bước Thiết kế Ca sử dụng (Use Case Design) là gì? (Chọn ba.)
* Vẽ chi tiết cách các đối tượng gửi tin nhắn giao tiếp chạy tính năng.
Đảm bảo vẽ đầy đủ sơ đồ các lớp tham gia cho từng tính năng.
* Tinh chỉnh chi tiết các hàm khai báo trong lớp thiết kế.
* Tinh chỉnh chi tiết các hàm trong phân hệ thiết kế hoặc giao diện của chúng.

Sản phẩm đầu ra của hoạt động Thiết kế Ca sử dụng là gì?
Các lớp phân tích sơ khai.
Các lớp thiết kế chi tiết.
Các giao diện kết nối (interfaces).
* Mô hình Thiết kế (Design Model) hoàn thiện.

Thành phần nào sau đây là một ví dụ về Môi trường Thực thi (Execution Environment)?
Thiết bị chia mạng (Gigabit network switch).
Mạng riêng ảo (VPN).
* Máy chủ ứng dụng J2EE Application Server (hoặc Docker, JVM).
Thiết bị máy tính cầm tay (handheld computer).

Mục đích của hoạt động Hợp nhất các Lớp và Phân hệ (Unify Classes and Subsystems) là gì?
Đảm bảo mỗi phân hệ đều có một thành phần chạy tương ứng.
Đảm bảo mỗi lớp chỉ được nằm trong tối đa một phân hệ.
* Loại bỏ trùng lặp, đảm bảo nhiệm vụ của các lớp thiết kế không chồng chéo lên nhau.
Đảm bảo mỗi giao diện đều có mã nguồn rõ ràng.

Quy tắc tốt nhất để vẽ phân hệ (subsystem) trên sơ đồ tuần tự là gì?
* Các tin nhắn gửi vào phân hệ phải khớp đúng với các hàm khai báo ở giao diện của phân hệ đó.
Tin nhắn từ phân hệ gửi ra phải tương ứng với các hàm của thành phần phân hệ.
Phải vẽ thêm sơ đồ tuần tự phụ để mô tả chi tiết code chạy bên trong phân hệ.
Nên vẽ phân hệ bằng cách dùng các thành phần của nó.

Thành phần nào sau đây là ví dụ về một đầu nối (connector) trên sơ đồ triển khai?
Ổ đĩa cứng chuẩn SCSI.
Bộ tích điện dự phòng (UPS).
* Giao thức HTTP (HTTP protocol).
Thư viện JDBC dùng kết nối database.

Thành phần nào sau đây là một thiết bị vật lý (device)?
Phần mềm máy chủ cơ sở dữ liệu (database server).
Phần mềm máy chủ web (web server).
Mạng riêng ảo (VPN).
* Thiết bị máy tính cầm tay (handheld computer).

Góc nhìn nào trong mô hình 4+1 View là trọng tâm của hoạt động Mô tả Phân phối (Describe Distribution)?
Góc nhìn Logic (Logical View)
* Góc nhìn Triển khai (Deployment View)
Góc nhìn Ca sử dụng (Use Case View)
Góc nhìn Thực hiện (Implementation View)

Ba hoạt động nào là một phần của Thiết kế Lớp (Class Design)? (Chọn ba.)
* Vẽ các lớp và mối quan hệ để phục vụ trực tiếp cho việc viết code.
* Xác định và phân tích sự thay đổi trạng thái của các đối tượng có vòng đời phức tạp.
Tìm các lớp đại diện cho các khái niệm nghiệp vụ thực tế.
* Tinh chỉnh chi tiết các mối quan hệ, các hàm và các thuộc tính của lớp.

[image: SAD/images/page_24_img_1.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu D đại diện cho cái gì?
Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
* Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)

Sản phẩm chuyển giao (Artifacts) trong UML là những thực thể như thế nào?
Máy chủ chạy các phần mềm.
* File code, file thư viện (.jar, .war) được cài đặt trực tiếp lên thiết bị phần cứng.
Kết quả của việc vẽ chuyển đổi giữa các sơ đồ.
Các tệp tạm lưu trong bộ nhớ trình duyệt.

[image: SAD/images/page_28_img_1.jpeg]
Trong sơ đồ máy trạng thái, ký hiệu G đại diện cho cái gì?
Nhánh rẽ song song (fork)
Trạng thái bắt đầu (initial state)
Điểm quyết định rẽ nhánh (decision)
Mũi tên chuyển trạng thái (transition)
Trạng thái kết thúc (final state)
Sự kiện kích hoạt (event)
* Trạng thái thông thường (state)
Điều kiện bảo vệ (guard condition)