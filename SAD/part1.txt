Phát biểu nào sau đây là đúng về UML?
A. UML là một quy trình để phát triển phần mềm.
B. UML là ngôn ngữ phụ thuộc quy trình dùng để trực quan hóa phần mềm.
* C. UML là ngôn ngữ vẽ sơ đồ (mô hình hóa) dùng để thiết kế bản vẽ phần mềm.
D. UML là một ngôn ngữ lập trình trực quan.

Lớp có cấu trúc (structured class) khác với lớp thông thường ở 3 điểm nào? (Chọn 3)
* A. Nó xác định rõ ranh giới tự bảo vệ của mình thông qua vỏ bọc đóng gói.
* B. Nó kết nối với bên ngoài thông qua các cổng cắm (ports).
* C. Nó thể hiện rõ các vai trò mà lớp đó đảm nhận.
D. Nó tự định nghĩa các tin nhắn truyền giữa nó và lớp khác.

Đặc điểm của một lớp có cấu trúc (structured class) là gì?
A. Phải có một giao diện (interface) riêng cho từng vai trò nó đảm nhận.
B. Chỉ đóng đúng một vai trò duy nhất, không quan tâm có bao nhiêu đối tượng tương tác với nó.
* C. Có thể đóng nhiều vai trò khác nhau tùy thuộc vào đối tượng đang tương tác với nó.
D. Bị giới hạn ở một vai trò, nhưng có thể có nhiều giao diện.

Phát biểu nào sau đây là đúng về quy trình phát triển lặp (iterative)?
* A. Việc kiểm thử và tích hợp code diễn ra liên tục trong tất cả các vòng lặp.
B. Mỗi vòng lặp tập trung vào việc hoàn thành một phần các tính năng được chọn.
C. Quy trình khuyến khích lấy phản hồi của người dùng ở những vòng lặp cuối cùng.
D. Quy trình dựa trên việc chia nhỏ chức năng của hệ thống.

Hai phát biểu nào sau đây là đúng về giao diện (interface)? (Chọn 2)
* A. Một giao diện nên có một mục đích sử dụng rõ ràng và duy nhất.
B. Một giao diện nên chứa càng nhiều hàm càng tốt để các đối tượng dùng chung.
C. Giao diện nên dùng để giới hạn các hàm hiển thị ra cho bên gọi (client) sử dụng.
* D. Một lớp có thể thực hiện nhiều giao diện khác nhau tùy theo nhu cầu.

Trọng tâm của giai đoạn Phân tích (Analysis) là gì?
A. Chuyển đổi các yêu cầu chức năng thành mã nguồn (code).
B. Chuyển đổi các yêu cầu thành thiết kế chi tiết của hệ thống.
C. Chuyển các khái niệm thực tế thành các đối tượng lập trình chi tiết.
* D. Chuyển các yêu cầu của người dùng thành các khái niệm phần mềm ở mức khái quát.

Tại sao tính đóng gói (encapsulation) lại quan trọng? (Chọn 2)
A. Nó mô tả mối quan hệ cha-con (thừa kế) giữa các lớp.
* B. Nó nhóm các thuộc tính (dữ liệu) và phương thức (hành vi) liên quan vào chung một đối tượng.
C. Nó cho phép các đối tượng bên ngoài tự do sửa đổi các thuộc tính ẩn (private) của đối tượng.
* D. Nó ngăn chặn các đối tượng bên ngoài thay đổi trực tiếp các thuộc tính bên trong của đối tượng.

Lớp phân tích (analysis class) là gì?
* A. Là những phác thảo ý tưởng sơ khai về cấu trúc hệ thống, thường thay đổi theo thời gian và hiếm khi giữ nguyên vẹn cho đến khi viết code thực tế.
B. Là các lớp chưa hoàn thiện, đòi hỏi lập trình viên phải định nghĩa đầy đủ các hàm trước khi viết code.
C. Là các lớp nằm trong Mô hình miền (Domain Model) của hệ thống dưới dạng sơ đồ UML.
D. Là bản vẽ thiết kế giao diện người dùng được tạo ra trong giai đoạn Phân tích.

Một kiến trúc sư xem xét hai lớp: Lớp thứ nhất có các hàm getName(), getSize(), getTotal() và findAverage(). Lớp thứ hai có các hàm getName(), getSize(), findAverage(), findMinimum() và findMaximum(). Cả hai lớp đều chung một lớp cha (superclass). Những hàm nào có khả năng cao nhất sẽ được đưa lên lớp cha?
* A. getName(), getSize() và findAverage()
B. findMaximum(), findMinimum(), getSize() và getTotal()
C. getName(), findAverage() và findMaximum()
D. getName(), getSize(), getTotal() và findAverage()

Khi xây dựng Mô hình phân tích (Analysis Model), kiến trúc sư cần tập trung vào điều gì nhất?
A. Cấu hình phần cứng dùng để triển khai hệ thống.
* B. Cách hoạt động và hành vi của các đối tượng trong hệ thống.
C. Quá trình phát triển các lớp phân tích thành các lớp thiết kế (design classes).
D. Các yêu cầu phi chức năng về hiệu suất của hệ thống.

Giao diện yêu cầu (required interface) của một thành phần dùng để làm gì?
A. Cung cấp dịch vụ cho các bên gọi ẩn danh.
* B. Khai báo các dịch vụ mà thành phần này cần đòi hỏi từ các bên cung cấp ẩn danh khác để chạy được.
C. Khai báo các dịch vụ mà thành phần này cung cấp cho các bên gọi ẩn danh.
D. Phơi bày các hàm bắt buộc mà bên gọi phải sử dụng.

Trong sơ đồ tuần tự (sequence diagram), mỗi thông điệp (message) gửi đi tương ứng với thành phần nào trên sơ đồ máy trạng thái (state machine diagram)?
A. Một điểm quyết định (choice point) trên sơ đồ trạng thái.
* B. Một bước chuyển đổi trạng thái (transition) trên sơ đồ trạng thái.
C. Một trạng thái (state) trên sơ đồ trạng thái.
D. Trạng thái bắt đầu (initial state).

Việc xác định bội số (multiplicity) trên các mối quan hệ giúp trả lời hai câu hỏi nào? (Chọn 2)
* A. Mối quan hệ liên kết này là bắt buộc hay tự nguyện (tùy chọn)?
* B. Một đối tượng thuộc kiểu này có thể liên kết với tối đa bao nhiêu đối tượng thuộc kiểu khác?
C. Một đối tượng thuộc kiểu này có được phép tương tác với các đối tượng thuộc kiểu khác không?
D. Mối quan hệ giữa các đối tượng này là vĩnh viễn hay tạm thời?

Hai lưu ý quan trọng khi vẽ sơ đồ trạng thái (state diagram) là gì? (Chọn 2)
* A. Mỗi khi đối tượng nhận được một thông điệp (message), trạng thái của nó có thể thay đổi.
B. Mỗi khi đối tượng nhận được một thông điệp, trạng thái của nó bắt buộc phải thay đổi.
* C. Mỗi khi trạng thái thay đổi, sẽ có một mũi tên chuyển trạng thái (transition) tương ứng.
D. Sự thay đổi trạng thái có thể diễn ra mà không cần mũi tên chuyển trạng thái.

Phát biểu nào sau đây là đúng về hiện tượng phụ thuộc vòng (circular dependency)?
A. Chúng không ảnh hưởng gì đến thiết kế hệ thống.
B. Chúng bị cấm hoàn toàn trong mọi biểu đồ UML.
* C. Cần phải tránh hiện tượng phụ thuộc chéo vòng quanh giữa các gói (packages).
D. Hiện tượng này chỉ ảnh hưởng khi dự án có nhiều hơn hai gói.

Mục đích chính của hoạt động Phân tích kiến trúc (Architectural Analysis) là gì?
A. Chi tiết hóa thiết kế chi tiết của toàn bộ hệ thống.
B. Đánh giá và kiểm duyệt lại kiến trúc hiện tại của hệ thống.
* C. Phác thảo ra một khung xương kiến trúc thử nghiệm (candidate architecture) cho hệ thống.
D. Xác định và phân chia các tầng (layers) trong kiến trúc.

Khi các giao diện (interfaces) giữa hai lớp được xác định từ sơ đồ tuần tự, các cổng cắm (ports) sẽ được định nghĩa dựa vào đâu?
* A. Các giao diện (interfaces)
B. Các phương thức mà lớp đó triển khai.
C. Yêu cầu của người dùng hệ thống.
D. Các thuộc tính được truyền đi trên sơ đồ tuần tự.

Phát biểu nào sau đây là đúng về các thuộc tính (attributes) của lớp?
A. Giá trị của thuộc tính không thể thay đổi sau khi đối tượng được tạo ra.
* B. Giá trị thuộc tính có thể khác nhau giữa các đối tượng khác nhau của cùng một lớp.
C. Thuộc tính chỉ được phép mang các kiểu dữ liệu nguyên thủy (như số, chữ).
D. Mọi lớp thiết kế bắt buộc phải định nghĩa các thuộc tính.

Ba mục đích chính của Phân tích và Thiết kế (Analysis and Design) là gì? (Chọn 3)
A. Cung cấp bối cảnh tổ chức hoạt động của dự án phần mềm.
* B. Chuyển đổi các yêu cầu chức năng thành bản vẽ thiết kế hệ thống sắp xây dựng.
* C. Xây dựng một khung kiến trúc mạnh mẽ và ổn định cho hệ thống.
D. Xác định phạm vi của hệ thống và mô tả chi tiết những gì hệ thống phải làm.
* E. Điều chỉnh thiết kế để phù hợp với môi trường cài đặt thực tế (mạng, database,...).

Các đối tượng có tính đa hình (polymorphic) có đặc điểm gì?
A. Bắt buộc phải có chung tất cả các thuộc tính bên trong.
B. Chia sẻ chung các phương thức và có cách xử lý các phương thức đó giống hệt nhau.
C. Chỉ có thể được hiện thực hóa thông qua việc khai báo các giao diện (interfaces).
* D. Có thể có các phương thức cùng tên nhưng cách xử lý thực tế bên trong lại khác nhau.

Nguyên tắc tốt nhất khi lồng ghép (nesting) các lớp có cấu trúc (structured classes) là gì?
A. Chỉ nên giới hạn việc lồng ghép các lớp có cấu trúc ở mức tối đa hai cấp trên mỗi biểu đồ.
B. Nên hiển thị tất cả các lớp con có cấu trúc bên trong trên một biểu đồ duy nhất để thấy rõ độ sâu.
* C. Chỉ hiển thị lồng ghép ở một cấp độ duy nhất, và cho phép người dùng mở biểu đồ phụ để xem cấp độ tiếp theo.
D. Việc lồng ghép phụ thuộc hoàn toàn vào quyết định của kiến trúc sư phần mềm trong giai đoạn viết code.

Trong hoạt động Phân tích kiến trúc, để giảm độ phức tạp và cải thiện tính đồng bộ của hệ thống, kiến trúc sư nên định nghĩa thành phần nào?
A. Các quy tắc viết code (coding rules).
B. Các hiện thực hóa ca sử dụng (use-case realizations).
* C. Các cơ chế phân tích (analysis mechanisms).
D. Các cơ chế thiết kế (design mechanisms).

Hai đặc điểm cơ bản nào mà mọi đối tượng (objects) đều sở hữu? (Chọn 2)
A. Các kiểu dữ liệu nguyên thủy (primitives).
* B. Trạng thái (thông tin dữ liệu) và hành vi (phương thức hoạt động).
C. Các giao diện kết nối (interfaces).
* D. Một định danh duy nhất (để phân biệt với đối tượng khác).

Lớp phân tích (analysis class) thường sẽ được phát triển thành các thành phần nào trong giai đoạn thiết kế? (Chọn 2)
* A. Lớp thiết kế (design class).
* B. Phân hệ thiết kế (design subsystem).
C. Hiện thực hóa ca sử dụng (use-case realization).
D. Gói thiết kế (design package).
E. Kiến trúc hệ thống (architecture).

Điều gì sẽ xảy ra khi ta thay đổi cấu trúc hoặc phương thức của một lớp cha (superclass)?
* A. Tất cả các lớp con (subclasses) kế thừa từ lớp cha đó đều nhận sự thay đổi này.
B. Tất cả các phương thức của lớp con bắt buộc phải được viết lại.
C. Các phương thức riêng biệt của lớp con không liên quan đến lớp cha cũng phải thay đổi theo.
D. Chỉ các phương thức công khai của lớp cha mới được các lớp con kế thừa.

Hai phát biểu nào sau đây là đúng về hiện thực hóa ca sử dụng (use-case realization)? (Chọn 2)
A. Nó liệt kê chi tiết các bước thực hiện của một ca sử dụng bằng văn bản.
* B. Nó giúp theo dõi mối liên hệ từ thiết kế kỹ thuật quay trở lại các yêu cầu ban đầu của khách hàng.
C. Nó được xây dựng bởi Chuyên viên phân tích hệ thống (System Analyst).
* D. Nó mô tả cách các đối tượng thiết kế tương tác và gửi tin nhắn cho nhau để thực hiện chức năng.

Trạng thái đơn (simple state) trong sơ đồ trạng thái là gì?
A. Trạng thái đỉnh (Top State).
B. Trạng thái phức hợp chứa các trạng thái con bên trong.
* C. Trạng thái độc lập, không chứa bất kỳ trạng thái con nào khác bên trong.
D. Trạng thái kết thúc (Final State).

Phát biểu nào sau đây là đúng về việc nhóm các phần tử vào một gói (package)?
* A. Các phần tử trong cùng một gói nên có chung một mục đích logic hoặc chức năng liên quan.
B. Mỗi gói chỉ nên chứa một số lượng nhỏ các phần tử để tránh gây nhầm lẫn.
C. Các gói chỉ nên được áp dụng trong các dự án lớn có cấu trúc phức tạp.
D. Một gói không được phép chứa các gói con khác bên trong nó.

Ba danh mục của cơ chế kiến trúc (architectural mechanism) là gì? (Chọn 3)
* A. Cơ chế phân tích (analysis mechanisms)
B. Cơ chế yêu cầu (requirement mechanisms)
* C. Cơ chế thực hiện (implementation mechanisms)
* D. Cơ chế thiết kế (design mechanisms)

Trong sơ đồ tuần tự (sequence diagram), các tương tác giữa các đối tượng tham gia giúp xác định điều gì?
A. Chỉ xác định các dịch vụ được cung cấp bởi một giao diện (interface).
B. Chỉ xác định các dịch vụ được yêu cầu bởi một giao diện.
* C. Xác định cả các dịch vụ cung cấp và dịch vụ yêu cầu của các giao diện tương ứng.
D. Xác định tên gọi của giao diện.

Góc nhìn (View) nào tập trung vào cấu trúc mã nguồn và cách tổ chức các file code của hệ thống?
A. Góc nhìn logic (Logical View)
* B. Góc nhìn thực hiện (Implementation View)
C. Góc nhìn tiến trình (Process View)
D. Góc nhìn ca sử dụng (Use-Case View)

Phát biểu nào sau đây là đúng về đối tượng hoạt động (active object)?
* A. Là đối tượng chạy độc lập, tự quản lý luồng thực thi riêng và giao tiếp bất đồng bộ (không cần chờ đợi) với đối tượng khác.
B. Là đối tượng hoàn toàn không lưu trữ trạng thái (state).
C. Sự hoạt động của nó phụ thuộc hoàn toàn vào luồng thực thi chung của toàn bộ hệ thống.
D. Nó không có cấu trúc cố định để lập trình viên dễ dàng thao tác lớp.

Trong hoạt động Phân tích ca sử dụng (Use Case Analysis), trách nhiệm nào thường được phân bổ cho lớp điều khiển (control class)?
* A. Hành vi đặc thù của một ca sử dụng cụ thể hoặc điều phối luồng xử lý chính của chức năng đó.
B. Hành vi lưu trữ và quản lý dữ liệu được đóng gói bên trong lớp.
C. Hành vi giao tiếp và tương tác trực tiếp với tác nhân bên ngoài (actor).
D. Hành vi xử lý các quy tắc nghiệp vụ chung cho toàn hệ thống.

Ba nguồn chính để xác định các khái niệm cốt lõi (Key Abstractions) của hệ thống là gì? (Chọn 3)
* A. Tri thức nghiệp vụ thực tế của dự án.
* B. Tài liệu yêu cầu phần mềm của khách hàng.
C. Các lớp thiết kế chi tiết được tạo ra khi viết code.
* D. Thuật ngữ định nghĩa của hệ thống.

Đối tượng có tên (named object) được biểu diễn như thế nào trong sơ đồ UML?
* A. Matt: Employee
B. :Employee
C. Matt::Employee
D. ::employee

Trong Phân tích ca sử dụng, ta nên chọn một thuộc tính (attribute) thay vì tạo một lớp (class) khi thông tin đó:
* A. Chỉ cần truy cập qua các phương thức lấy/ghi giá trị đơn giản (get/set) hoặc biến đổi cơ bản.
B. Có liên quan mật thiết đến các thực thể nghiệp vụ lớn trong hệ thống.
C. Cần được ẩn giấu hoàn toàn khỏi các phần khác và không được chia sẻ.
D. Được tham chiếu và sử dụng lại nhiều lần trong chức năng.

Góc nhìn (View) nào sau đây KHÔNG thuộc mô hình kiến trúc RUP 4+1 View Model?
A. Góc nhìn logic (Logical View)
* B. Góc nhìn phân phối (Distribution View)
C. Góc nhìn ca sử dụng (Use-Case View)
D. Góc nhìn tiến trình (Process View)

Hai chức năng cốt lõi của việc mô hình hóa trực quan (visual modeling) là gì? (Chọn 2)
A. Tạo ra một mô hình duy nhất bao quát toàn bộ các góc nhìn của hệ thống.
* B. Giúp các thành viên trong nhóm phát triển trao đổi và hiểu thống nhất ý tưởng thiết kế.
C. Tài liệu hóa các quyết định thiết kế trực tiếp ngay trong mã nguồn (code).
* D. Tài liệu hóa cấu trúc và cách hoạt động của hệ thống trước khi bắt tay vào viết code.

Trong Phân tích ca sử dụng, tại sao các cơ chế phân tích (analysis mechanisms) lại được sử dụng?
* A. Để giảm độ phức tạp và tăng tính nhất quán bằng cách đại diện ngắn gọn cho các hành vi phức tạp phổ biến.
B. Để viết tắt các tác vụ lặp đi lặp lại nhằm tăng tốc độ vẽ hiện thực hóa ca sử dụng.
C. Để gom nhóm các tác vụ lập trình chung giúp kiến trúc sư dễ bàn giao công việc cho lập trình viên.
D. Để kiểm tra xem lập trình viên đã làm đúng các yêu cầu phân tích của kiến trúc sư hay chưa.

Phát biểu nào sau đây là đúng về mẫu thiết kế (pattern)?
A. Các mẫu chỉ tồn tại và được áp dụng ở cấp độ Thiết kế chi tiết (Design).
* B. Mẫu cung cấp một giải pháp chung đã được kiểm chứng cho một vấn đề thiết kế phổ biến.
C. Các mẫu chỉ tồn tại và được áp dụng ở cấp độ Kiến trúc hệ thống (Architecture).
D. Mẫu thiết kế chỉ được nghiên cứu và sử dụng trong giai đoạn Tinh chế (Elaboration).

Trong Phân tích ca sử dụng, tại sao đôi khi cần phải bổ sung thêm các mô tả chi tiết cho ca sử dụng?
* A. Mô tả bằng văn bản của ca sử dụng đôi khi không đủ chi tiết để xác định các lớp phân tích và đối tượng tham gia.
B. Luồng sự kiện mô tả trong ca sử dụng quá phức tạp để lập trình viên có thể viết code trực tiếp.
C. Lập trình viên cần biết luồng sự kiện nào của ca sử dụng đang được phát triển trong vòng lặp hiện tại.
D. Kiến trúc sư cần sửa lại các yêu cầu bị viết sai của khách hàng trước khi bàn giao cho thiết kế.

Trong Phân tích ca sử dụng, mục đích của hoạt động Hợp nhất các lớp phân tích (Unify Analysis Classes) là gì?
* A. Đảm bảo mỗi lớp phân tích đại diện cho một khái niệm duy nhất, rõ ràng và không có trách nhiệm chồng chéo với lớp khác.
B. Tập hợp toàn bộ các lớp phân tích từ các ca sử dụng để chuyển đổi trực tiếp vào Mô hình thiết kế.
C. Đảm bảo trách nhiệm của mỗi lớp được nhất quán và lớp chỉ thực hiện đúng một nhiệm vụ duy nhất.
D. Xây dựng cây thừa kế ban đầu để phân nhóm các lớp phân tích có liên quan với nhau.

Ba quy trình/phương pháp phát triển phần mềm nào phù hợp nhất để áp dụng UML? (Chọn 3)
* A. Hướng ca sử dụng (Use-case driven)
B. Dựa trên mô hình thác nước truyền thống (Waterfall)
* C. Lặp và tăng dần (Iterative and incremental)
* D. Lấy kiến trúc làm trung tâm (Architecture-centric)
E. Lấy yêu cầu làm trung tâm (Requirements-centric)

Các tầng kiến trúc (architectural layers) thường được mô hình hóa trong UML bằng cách sử dụng thành phần nào?
* A. Một gói (package) được gắn nhãn stereotype <<Layer>>
B. Một phân hệ (subsystem) được gắn nhãn stereotype <<Layer>>
C. Một lớp phức hợp (composite class) được gắn nhãn stereotype <<Layer>>
D. Sơ đồ lớp (class diagram) được đặt tên là Layer

Trong sơ đồ trạng thái, một trạng thái có hai chuyển trạng thái: một chuyển trạng thái nội bộ (internal transition) và một chuyển trạng thái bên ngoài (external transition). Loại chuyển trạng thái nào sẽ kích hoạt hành động thoát (exit action) của trạng thái đó?
A. Chỉ chuyển trạng thái nội bộ.
* B. Chỉ chuyển trạng thái bên ngoài.
C. Cả hai loại chuyển trạng thái đều không kích hoạt hành động thoát.
D. Cả hai loại chuyển trạng thái đều kích hoạt hành động thoát.

Hai phát biểu nào sau đây là đúng về quy trình hướng ca sử dụng (use-case driven)? (Chọn 2)
* A. Ca sử dụng rất trực quan, ngắn gọn nên dễ hiểu đối với tất cả các bên tham gia.
* B. Ca sử dụng giúp liên kết và đồng bộ hóa nội dung giữa các mô hình phát triển khác nhau.
C. Ca sử dụng cung cấp một danh sách đầy đủ tất cả các yêu cầu chức năng và phi chức năng.
D. Ca sử dụng mô tả cách thức hệ thống sẽ triển khai hành vi đó bằng code như thế nào.

Chức năng cốt lõi của lớp thực thể (entity class) là gì?
* A. Lưu trữ và quản lý thông tin, dữ liệu chạy trong hệ thống.
B. Đóng vai trò làm đại diện (proxy/surrogate) cho các tác nhân bên ngoài tương tác với hệ thống.
C. Bọc các câu lệnh truy vấn cơ sở dữ liệu quan hệ của hệ thống.
D. Định tuyến và trung chuyển thông tin giữa các tầng khác nhau trong hệ thống.

Cơ chế mục đích chung (general purpose mechanism) của UML để tổ chức các phần tử thành nhóm là gì?
A. Sơ đồ lớp (class diagram)
B. Sơ đồ hoạt động (activity diagram)
* C. Gói (package)
D. Sơ đồ phức hợp (composite diagram)

Trong Phân tích ca sử dụng, nên vẽ bao nhiêu sơ đồ tương tác (sơ đồ tuần tự hoặc sơ đồ truyền thông) cho mỗi ca sử dụng?
* A. Vẽ đủ số lượng sơ đồ để đảm bảo tất cả trách nhiệm của các lớp tham gia được định nghĩa và hầu hết các luồng sự kiện được kiểm tra.
B. Chỉ cần vẽ một sơ đồ cho luồng sự kiện chính và một sơ đồ cho mỗi luồng sự kiện rẽ nhánh.
C. Vẽ một sơ đồ tuần tự và một sơ đồ truyền thông cho luồng chính, cùng cặp sơ đồ tương tự cho mỗi luồng rẽ nhánh.
D. Chỉ vẽ một sơ đồ cho mỗi ca sử dụng, các luồng rẽ nhánh sẽ được thể hiện thông qua các phân đoạn tương tác con (interaction occurrences).

Trong Phân tích ca sử dụng, chức năng chính của lớp biên (boundary class) là gì?
* A. Cách ly các tác nhân bên ngoài (actors, hệ thống ngoài) khỏi cơ chế xử lý bên trong của hệ thống và ngược lại.
B. Đại diện cho giao diện trực quan của các màn hình ứng dụng.
C. Định nghĩa các dịch vụ (services) được cung cấp bởi tầng Nghiệp vụ (Business layer).
D. Làm điểm truy cập đầu tiên vào Mô hình dữ liệu đối tượng cốt lõi của hệ thống.